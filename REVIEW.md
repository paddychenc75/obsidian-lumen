# 过审与实现纪要

日期：2026-08-18  
稿：`preview.html` + 本轮 `theme.css` 架构修复

## 董事会（设计稿）

| 角色 | 结论 | 开工 |
|---|---|---|
| 产品 | 通过 | 批准 |
| UI | 有条件通过 | 批准 |
| UX | 有条件通过 | 批准 |
| 独立视觉 | 有条件通过 | 批准 |

实机窗口此前**未过**设计师终审。旧纪要把网页文档流模型套到了 Obsidian 工作区上，导致“检查通过”与实机截图明显冲突。本轮已按 Obsidian 1.13 的真实 DOM 和计算样式修复。

## 本轮改动

1. **状态栏改为动态锚定**：`position: relative` 会让状态栏成为 `.app-container` 的普通 flex 子项，挤压整个工作区；同时 Obsidian 1.13 并不提供旧稿假设的 `--left-sidebar-width`。现用 CSS Anchor Positioning 锚定 `.mod-root`，左右侧栏展开、折叠或拖宽时都跟随真实编辑列。
2. **恢复状态栏空间**：根工作区预留状态栏高度，避免绝对定位后的状态栏盖住编辑器底部。
3. **侧栏不再溢出**：外边距之前叠加在 `height: 100%` 上，侧栏底部超出窗口 10px；现按上下 inset 扣减高度。
4. **恢复拖宽热区**：`.mod-left-split/.mod-right-split { overflow: hidden }` 会裁掉侧栏 resize handle。外层改为可见，只在内部 `.workspace-tabs` 裁圆角。
5. **Ribbon 尺寸一致**：修复 Obsidian 轨道挤压，实机岛宽与 52px 设计基线一致。
6. **深色玻璃重量正确**：`lumen-glass-thin/thick` 原来无视主题模式，深色下仍混入白色填充；现分别定义 light/dark 材质。
7. **覆盖层可辨识**：遮罩由 36% 提升到 46%，让命令面板和设置与背景形成稳定层级。
8. **移动底栏补齐材质与宽度**：补上高光、边框、阴影和玻璃文字色；覆盖 Obsidian 的 `--navbar-width/max-width`，390px 视口下保持左右各 10px 安全边距，不再缩成偏左的 316px。
9. **键盘焦点补齐**：文件夹、树节点、菜单、输入框、文本域和下拉框现在都有可见 focus ring。
10. **窄窗口降密度**：700px 以下收紧 inset、行高与正文 gutter，避免桌面窄窗横向拥挤。
11. **辅助模式优先级修复**：厚玻璃不再覆盖“降低透明度/增强对比”；组合启用后强制实色、`blur: 0`。
12. **减少动态效果真正生效**：将 reduced-motion 规则移到通用 transition 之后，避免被后写的 180ms 动画覆盖。
13. **统一间距与圆角令牌**：全局收敛为 `4/8/12/16/24/32px` 间距和 `8/12/16/24px` 圆角；工作区 chrome inset 统一为 8px。
14. **设置与命令面板同源**：设置外壳和命令面板共用同一玻璃填充；纸面保持实色。设置行 16px padding、导航和命令项 4px gap、命令输入 44px，不再混用 2/6/10/20/24px。
15. **更新说明页修复**：折叠 Callout 从“纸色背景 + 一截彩色弧线”改为完整语义色卡片；顶部 padding、卡片间距、分隔线与标题间距同步收紧。
16. **状态栏去空轨并减重**：状态栏改为锚定编辑列右下角的 48px 内容型玻璃岛，纸面延伸到底部；另用中性补偿层抵消底部金色墙纸，避免比侧栏更浑、更重。
17. **窗口接缝与标签修复**：Ribbon 玻璃延伸到 macOS 交通灯下方；active tab 只保留一层选中材质；chrome inset 从 8px 收紧到 4px，使侧栏到纸面的视觉空带由 16px 降到 8px。
18. **侧栏圆角与底边对齐**：左右侧栏外壳由 16px 收敛到与纸面一致的 12px；root tab container 的上下 margin 从遗留的 8px 改为统一 4px，三栏底边完全对齐。

## 验证

| 项 | 方式 | 结果 |
|---|---|---|
| CSS 括号/结构 | 脚本扫描 | 通过，无未闭合 |
| Obsidian 开发错误 | `dev:errors` | 0 |
| 左侧栏展开 | 实机几何 | 侧栏底部 1074 = 内容底线 1074 |
| 双侧栏展开 | 实机几何 | 状态栏 `x=390..1398`，编辑列内边界 `x=390..1398` |
| 侧栏 resize handle | 计算样式 | 外层 `overflow: visible`，内部裁圆角 |
| 状态栏 | 计算样式 | `position: absolute` + `position-anchor: --lumen-editor-column` |
| 浅色 / 深色 | 实机截图 | 两套纸面、玻璃文字和边界均正常 |
| 侧栏组合 | 6 组动态回归 | 左开右收、双开、左收右开、双收、300/420px 拖宽全部通过 |
| 1024×700 双侧栏 | CDP 真实视口 | 无重叠、无文档横向溢出，状态栏仍贴合编辑列 |
| 700×700 窄窗口 | CDP 真实视口 | 6px inset、34px 行高、32px 控件生效，无横向溢出 |
| 390×844 移动端 | CDP 真实视口 | 底栏 370×52px，四周 10px；按钮热区 57×50px，无横向溢出 |
| 命令面板 | CDP 键盘交互 | `⌘P`、方向键导航、`Esc` 关闭均通过 |
| 独立设置窗口 | 真实窗口 DOM | 900×700 浅/深色纸面正确，无横向溢出 |
| 降透明 + 厚玻璃 | 组合状态 | 填充等于 solid、`blur: 0`、`backdrop: none` |
| 系统辅助偏好 | CDP media 模拟 | reduced-motion 为 `0s`，降低透明度与增强对比均生效 |
| 设置 + 命令面板叠放 | 独立窗口 DOM | 640px 面板居中、输入与结果零重叠、4px 行距、无横向溢出 |
| 设置浅色 / 深色 | 独立窗口 DOM | 外壳与命令面板玻璃值一致，纸面和选中态分别使用统一 token |
| 更新说明页 | 实机 DOM + 截图 | Callout 完整着色，首屏顶部减少 16px，卡片 8px、分隔线 16px、H2 32px |
| 紧凑状态岛 | 实机几何 | 48×32px，距编辑列右/下各 4px，完全落在纸面内 |
| 交通灯 / 标签 / 接缝 | 实机几何 | Ribbon 顶部 4px、active inner 透明无边框、两侧视觉空带各 8px |
| 侧栏圆角 / 三栏底边 | 实机几何 | 左右侧栏与纸面均为 12px；三者 bottom 均为 1080，差值 0 |

## 仍在的风险

- 状态栏动态对齐依赖 CSS Anchor Positioning；Obsidian 1.13 内置 Chromium 已支持。若未来切到不支持 anchor 的旧版 Chromium，会回退为 Obsidian 默认状态栏定位。
- 更新说明页的窄栏是 Obsidian 自己的 changelog 版式，不完全受 `--file-line-width` 控制。
- Obsidian 的主题热重载存在缓存延迟；开发期最终验收应重启演示库，不能只看切换主题后的瞬时结果。
